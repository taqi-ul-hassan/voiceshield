# VoiceShield — Kubernetes manifests
#
# Deploy order:
#   kubectl apply -f k8s/secret.yaml      # fill in real keys first
#   kubectl apply -f k8s/deployment.yaml
#   kubectl apply -f k8s/service.yaml
#   kubectl apply -f k8s/ingress.yaml     # optional — edit host

---
# ── Namespace ────────────────────────────────────────────────
apiVersion: v1
kind: Namespace
metadata:
  name: voiceshield
  labels:
    app.kubernetes.io/name: voiceshield
